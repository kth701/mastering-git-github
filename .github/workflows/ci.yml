
name: Build

on:
  push:
    branches: [ main ]
jobs:
  build:


    steps:
      - name: Checkout source code.
        uses: actions/checkout@v4

      - name: action/setup-node@v2
        with:
          node-version: '22'

